
public class queryAccount {
	
	//Takes account number as input, runs it through the bankSystem and bankServer, returns account balance
	public static double query(int acctnum) {
		return 1000.00;
	}
}
