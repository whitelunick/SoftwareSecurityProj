
public class login {
	
	//Takes username and password as input, passes them to the bankSystem and bankServer for verification, and confirms login
	public static void login(String user, String pass) {
		System.out.println("Logged in as " + user + " with password "+ pass);
	}
}
